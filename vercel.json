{
  "version": 2,
  "buildCommand": "mkdir -p public && cp -r assets index.html styles.css styles.fixed.css script.js login.html login.js mobile.html mobile.js manifest.webmanifest sw.js version.json vida-nova-fallback.json *.png *.svg *.xml public/ 2>/dev/null || true",
  "outputDirectory": "public",
  "functions": {
    "api/server.js": {
      "maxDuration": 30,
      "memory": 256
    }
  },
  "routes": [
    { "src": "/api/(.*)",     "dest": "/api/server.js" },
    { "src": "/version.json", "dest": "/api/server.js" },
    { "src": "/(.*)",         "dest": "/$1" }
  ]
}
