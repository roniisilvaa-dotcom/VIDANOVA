{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci --omit=dev",
  "functions": {
    "server.js": {
      "maxDuration": 30,
      "memory": 256
    }
  },
  "routes": [
    { "src": "/api/(.*)",     "dest": "/server.js" },
    { "src": "/version.json", "dest": "/server.js" },
    { "src": "/(.*)",         "dest": "/$1" }
  ]
}
